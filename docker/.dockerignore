# =============================================================================
# Docker Ignore File
# =============================================================================
# This file specifies which files and directories should be excluded
# from the Docker build context to speed up builds and reduce image size.
# =============================================================================

# Dependencies (will be installed inside container)
node_modules
**/node_modules

# Build outputs (will be created inside container)
dist
**/dist

# Development files
.git
.gitignore
.env*.local
*.log

# IDE
.vscode
.idea
*.swp
*.swo

# Test files
**/*.spec.ts
**/*.spec.tsx
**/*.test.ts
**/*.test.tsx
coverage
.nyc_output

# Documentation (not needed in container)
*.md
!README.md

# Docker files (prevent recursive context)
docker/
Dockerfile*

# OS files
.DS_Store
Thumbs.db

# Temporary files
tmp
temp
*.tmp

# NX cache
.nx
.cache

